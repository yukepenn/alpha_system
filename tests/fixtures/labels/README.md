# Label Fixtures

The Python helpers in this directory create tiny deterministic synthetic
one-minute bars for ASV1-P10 label correctness tests. They are not market data
and are used only to test label schema, availability, session, and path logic.
