# FUTCORE-P07 Cross-Market AlphaSpec Drafts

This directory contains the value-free cross-market / relative-value AlphaSpec
batch for `ALPHA_FUTURES_CORE_ALPHA_PILOT_V1` phase `FUTCORE-P07`.

Contents:

- 16 JSON AlphaSpec draft payloads, one draft per file.
- Each payload uses only the governance `AlphaSpec` top-level fields.
- Each draft targets only `ES`, `NQ`, and/or `RTY`.
- Each draft declares cross-instrument `available_ts` alignment, bar/session
  alignment, lead/lag or residual/rotation/confirmation conventions,
  cross-market missingness diagnostics, cost-profile discipline, and duplicate
  exposure awareness.

These are draft research hypotheses for independent critique. They are not
approved StudySpecs, diagnostics, implementation requests, results, rejected
idea records, or promotion decisions.
