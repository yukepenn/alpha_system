# FLF-P07 Handoff

Campaign: `ALPHA_FEATURE_LABEL_FOUNDATION_V1`
Phase: `FLF-P07` - Transform / Window / Normalization Primitives
Lane: YELLOW
Executor: Codex

## Summary

Added the shared `alpha_system.features.primitives` package for causal
transform, trailing-window, return, volatility/range, ATR-style, normalization,
and session-reset primitives. The live package-root surface is keyed by
`available_ts` and is selected through FLF-P06 `TransformSpec`, `WindowSpec`,
and `NormalizationSpec` objects.

Centered and future-looking helpers are isolated in
`alpha_system.features.primitives.offline`; they require explicit
offline-only `WindowSpec` contracts and are rejected by the live primitive
builder. Synthetic no-trade rows and missing/quarantined BBO rows are surfaced
as primitive gaps rather than treated as observed trade/quote values.

## Executor Staging

Exact staged file paths by executor: none.

The executor did not run `git add`, `git commit`, `git push`, `git status`, or
`git diff`. Ralph should stage only explicit commit-eligible paths.

## Commit-Eligible Paths For Ralph

```text
README.md
docs/feature_label_foundation/PRIMITIVES.md
handoffs/ALPHA_FEATURE_LABEL_FOUNDATION_V1/FLF-P07.md
src/alpha_system/features/primitives/__init__.py
src/alpha_system/features/primitives/causal.py
src/alpha_system/features/primitives/offline.py
tests/no_lookahead/feature_label/test_primitives_causal.py
tests/unit/features/test_primitives.py
```

No `reviews/**`, `review.md`, or `verdict.json` artifact was created by the
executor.

## Validation Results

STOP check:

```text
test ! -e runs/2026-06-05T131433Z_ALPHA_FEATURE_LABEL_FOUNDATION_V1/STOP
  exit 0
  output: NO_STOP
```

Requested repo state:

```text
git status --short
  skipped: executor prompt explicitly forbids running git status
```

Import smoke:

```text
python -c "import alpha_system.features.primitives"
  exit 1
  stderr: ModuleNotFoundError: No module named 'alpha_system'
  reason: bare shell does not put the repo's src layout on PYTHONPATH

PYTHONPATH=src python -c "import alpha_system.features.primitives"
  exit 0
```

Harness smoke:

```text
python tools/verify.py --smoke
  exit 0
  output: none
```

Narrow unit test:

```text
python -m pytest tests/unit/features/test_primitives.py -q
  exit 0
  output: 8 passed in 0.05s
```

Feature/no-lookahead test suite:

```text
python -m pytest tests/unit/features tests/no_lookahead/feature_label -q
  exit 0
  output: 56 passed in 0.15s
```

Required doc presence:

```text
test -f docs/feature_label_foundation/PRIMITIVES.md
  exit 0
```

YELLOW-lane canaries:

```text
python tools/hooks/canary_runner.py
  exit 0
  output: All Frontier canaries passed.
```

Artifact audit:

```text
git ls-files runs
  exit 0
  output: none
```

Supplemental local checks:

```text
python -m ruff check src/alpha_system/features/primitives tests/unit/features/test_primitives.py tests/no_lookahead/feature_label/test_primitives_causal.py
  exit 0
  output: All checks passed!

python -m py_compile src/alpha_system/features/primitives/causal.py src/alpha_system/features/primitives/offline.py src/alpha_system/features/primitives/__init__.py
  exit 0

python -m py_compile tests/unit/features/test_primitives.py tests/no_lookahead/feature_label/test_primitives_causal.py
  exit 0
```

## Artifact Policy

- No staging or commit was performed by the executor.
- `git ls-files runs` returned no tracked `runs/` paths.
- The run-local handoff under `runs/**` is local-only and must not be staged.
- No raw market data, canonical data, feature values, label values, provider
  responses, parquet/arrow/feather files, DB/SQLite/WAL files, logs, caches, or
  heavy artifacts were created as commit-eligible artifacts.
- Python interpreter caches may be generated by validation commands; they remain
  local-only and must not be staged.

## Git Discipline

- Did not run `git add`, `git commit`, `git push`, `git status`, or `git diff`.
- Did not use `git add .` or `git add -A`.
- Did not force push.
- Left all changes unstaged for Ralph.
- No staged set was created by the executor; Ralph owns authoritative explicit
  staging and should confirm the staged set contains no `runs/` path before
  commit.

## DAG Metadata

- `parallel_safe: false`
- `must_run_alone: true`
- `merge_group: foundation`
- `ACTIVE_CAMPAIGN.md` was not written.
- No PR, CI wait, merge gate, merge, reviewer call, review artifact, verdict, or
  PASS marking was performed by the executor.

## Governance And Contracts

- FLF-P06 `TransformSpec`, `WindowSpec`, and `NormalizationSpec` are imported
  and consumed by the primitive dispatcher.
- Governance modules were not edited or duplicated.
- `FeatureSpec` live-window rejection remains the contract layer for live
  feature binding; primitive tests additionally assert the live primitive
  builder rejects centered/future/offline-only windows.

## Causality And Missingness

- Causal primitives sort by `available_ts`; output source timestamps are at or
  before the output `available_ts`.
- Unit and no-lookahead tests prove late-available values do not alter earlier
  results.
- Centered/future windows are offline-only in
  `alpha_system.features.primitives.offline` and are not exported from the
  package-root live primitive surface.
- Synthetic no-trade rows are emitted as gaps, not trade bars.
- Missing or quarantined BBO rows are emitted as gaps, not forward-filled quote
  values.
- Session reset helpers prevent trailing windows from crossing session
  boundaries when requested.

## README Snapshot

`README.md` now records a compact FLF-P07 snapshot: progress through `FLF-P07`
of 32, next work as the FLF-P08...FLF-P12 feature-family wave, the new
`alpha_system.features.primitives` module, the new
`docs/feature_label_foundation/PRIMITIVES.md` doc, no new command surface, and
unchanged local-only / accepted-DatasetVersion-only / no-provider / no-broker /
no-claims boundaries.

## Scope Confirmation

No concrete feature families, label families, materialized feature/label values,
FeatureStore/LabelStore writes, registry persistence, raw provider access,
external provider calls, broker/live/paper trading, order/account scope,
deployment, strategy/backtest/portfolio scope, alpha search, alpha claim,
tradability claim, profitability claim, governance-module edit, or
`ACTIVE_CAMPAIGN.md` write was introduced.

## Next Step

Ralph should perform authoritative explicit staging, handoff validation, review
routing, verdict parsing, and any merge-gate work outside this executor turn.
