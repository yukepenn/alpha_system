# AGENT-P05 Handoff — Tool Contract Registry and Structured Outputs

## Branch And Commit

- Branch: `auto/alpha_agent_factory_mvp/agent-p05-tool-contract-registry-and-structured-outputs`
- Starting HEAD observed by executor: `86eb685`
- Commits by Codex: none. Per executor instruction, Codex did not stage, commit, push, create a PR, merge, run reviewer, or create verdict/review artifacts.

## Files Changed

No files were staged by Codex. Ralph owns authoritative staging. Commit-eligible files created or edited by this executor are:

- `src/alpha_system/agent_factory/tools/__init__.py`
- `src/alpha_system/agent_factory/tools/contracts.py`
- `src/alpha_system/agent_factory/tools/registry.py`
- `src/alpha_system/agent_factory/tools/results.py`
- `tests/unit/agent_factory/tools/test_contracts.py`
- `tests/unit/agent_factory/tools/test_registry.py`
- `tests/unit/agent_factory/tools/test_results.py`
- `docs/agent_factory/TOOLS.md`
- `configs/agent_factory/tools/registry.toml`
- `README.md`
- `handoffs/ALPHA_AGENT_FACTORY_MVP/AGENT-P05.md`

Existing unchanged file:

- `tests/unit/agent_factory/tools/__init__.py`

Local-only generated cache files were created by Python imports/tests under `src/**/__pycache__/` and `tests/**/__pycache__/`. They are not commit-eligible and must not be staged.

## Scope Completed

- Added `AgentToolResult` with exactly the 16 required fields and enumerated statuses: `OK`, `BLOCKED`, `REJECTED`, `INCONCLUSIVE`, `WARN`.
- Added fail-closed value-free validation for result construction, rejecting bytes, dataframe/array-like objects, mutable collection fields, duplicate refs, raw/provider payload markers, embedded file contents, raw market record markers, and heavy artifact refs.
- Added `ToolContract` schema with tool name/group, allowed callers, AGENT-P04 permission cross-references, required inputs, `AgentToolResult` output schema, forbidden side effects, local-only artifact policy, failure states, required reviewer, reviewer-independence note, and `mvp`/`target`/`future` status.
- Added default-deny `ToolRegistry` loaded from `configs/agent_factory/tools/registry.toml`.
- Registered all candidate groups and tools: `registry`, `feature_label`, `runtime`, `review`, and `ledger_memory_promotion`.
- Added unit tests for field shape, raw/heavy rejection, contract completeness, P04 caller cross-reference validation, default-deny lookups, group coverage, and no direct registry-write behavior.
- Added `docs/agent_factory/TOOLS.md`.
- Updated the root `README.md` snapshot for AGENT-P05 and next phase `AGENT-P06`.

## Validation Run

- STOP check for `runs/2026-06-06T193514Z_ALPHA_AGENT_FACTORY_MVP/STOP`: pass, no STOP file present.
- `git status --short`: not run. The executor prompt explicitly forbids `git status`.
- `python -c "import alpha_system.agent_factory.tools.contracts, alpha_system.agent_factory.tools.registry, alpha_system.agent_factory.tools.results"`: fail in this executor environment with `ModuleNotFoundError: No module named 'alpha_system'` because the raw interpreter command does not include the repo `src` path and the package is not installed editable here.
- `PYTHONPATH=src python -c "import alpha_system.agent_factory.tools.contracts, alpha_system.agent_factory.tools.registry, alpha_system.agent_factory.tools.results"`: pass.
- `python tools/verify.py --smoke`: pass.
- `python -m pytest tests/unit/agent_factory/tools -q`: pass, `41 passed`.
- `test -f docs/agent_factory/TOOLS.md`: pass.
- `git ls-files runs`: pass, empty output.
- `python -m ruff format --check src/alpha_system/agent_factory/tools tests/unit/agent_factory/tools`: pass after formatting.
- `python -m ruff check src/alpha_system/agent_factory/tools tests/unit/agent_factory/tools`: pass after converting local enums to `StrEnum` and sorting imports.

During implementation, the first tools-unit pytest run failed because the module-level registry initialized before its config parser helper was defined. That ordering bug was fixed, and the suite passed afterward.

## Artifact Audit

- `git ls-files runs` returned empty.
- No `runs/**` file was created or modified by this executor.
- No review artifact, verdict JSON, PR, merge, staging, commit, broker/live/paper/order/account operation, provider call, runtime execution, alpha search, promotion, or deployment was performed.
- The only non-source local artifacts observed are Python `__pycache__` files generated by validation; they are cache artifacts and must remain unstaged.

## README Snapshot

`README.md` now states that `ALPHA_AGENT_FACTORY_MVP` has advanced through `AGENT-P05`, the next phase is `AGENT-P06`, and the new durable modules/docs are:

- `alpha_system.agent_factory.tools.contracts`
- `alpha_system.agent_factory.tools.registry`
- `alpha_system.agent_factory.tools.results`
- `docs/agent_factory/TOOLS.md`

The safety-boundary language remains contracts-only, local-first, accepted-DatasetVersion-only, no raw/provider access, no autonomous agent, and no broker/live/paper/order scope.

## Risks And Caveats

- The exact raw import command from the generated spec fails without `PYTHONPATH=src` in this executor environment. The scoped import succeeds with `PYTHONPATH=src`, pytest succeeds through the repo pytest configuration, and smoke succeeds.
- The P05 contract names intentionally do not edit AGENT-P04. Where P05 introduces a richer agent-facing name than P04, each contract records `permission_matrix_refs` to the existing P04 permission id that justifies each allowed caller.
- Tool implementations remain deferred to AGENT-P21. This phase added contracts only.

## Review Request Focus

Please review that:

- every candidate tool and group from the spec is present;
- every tool output schema is `AgentToolResult`;
- `AgentToolResult` is value-free and rejects raw/heavy payloads;
- default-deny behavior fails closed for unknown tools and disallowed callers;
- allowed callers are defensibly cross-referenced to the AGENT-P04 permission matrix;
- no runtime/governance/data primitives are imported or edited; and
- artifact policy is satisfied, especially no `runs/**` or cache artifacts in the curated stage set.

## Next Step

Ralph should explicitly stage only the commit-eligible paths above, run its lane-required checks and artifact guards, then route the required fresh Claude review. This executor does not mark the phase PASS.
