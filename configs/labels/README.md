# Label Configs

This directory is reserved for label-family configuration added by later
Feature/Label Foundation phases.

No label config values are defined in FLF-P02. Later family phases must place
family-scoped files under `configs/labels/families/<family>/`.
