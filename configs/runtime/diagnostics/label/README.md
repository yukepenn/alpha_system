# Label Diagnostics Runtime Config

This directory contains RT-P08 configuration scaffolding for
`alpha_system.runtime.diagnostics.label`.

The defaults are descriptive thresholds only. They do not authorize raw provider
access, label materialization, external calls, broker activity, paper or live
trading, deployment, or promotion decisions.
