# Split Diagnostics Runtime Configs

RT-P09 keeps split configuration small and synthetic-safe. These files define
metadata bucket names and session windows used by local tests and examples.
They do not contain market data, provider responses, materialized feature or
label payloads, local paths, secrets, databases, logs, caches, or runtime
artifacts.

