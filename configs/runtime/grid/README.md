# Bounded Grid Runtime Configs

This directory contains RT-P13 synthetic-safe bounded-grid defaults for
`alpha_system.runtime.grid`.

The files define finite budgets and partition defaults only. They do not contain
market data, feature values, label values, provider responses, secrets, local
paths, database files, logs, caches, heavy artifacts, or runtime run outputs.
