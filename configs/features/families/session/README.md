# Session Feature Family Config Placeholder

This directory is reserved for future declarative selections of the FLF-P10
Session / Calendar / Roll feature family.

FLF-P10 does not add materialization config, provider config, registry config,
broker config, live or paper execution behavior, or persisted feature values.
