# Feature Configs

This directory is reserved for feature-family configuration added by later
Feature/Label Foundation phases.

No feature config values are defined in FLF-P02. Later family phases must place
family-scoped files under `configs/features/families/<family>/`.
