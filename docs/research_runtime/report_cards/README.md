# Runtime Report Card Scaffolds

This directory holds tiny rendered Markdown scaffolds for the runtime report-card
shape. They are examples for source review only.

Scaffolds contain compact summaries and references, not provider payloads,
materialized feature/label tables, local DBs, logs, caches, or heavy report
bundles. Full runtime report bundles remain local-only when produced by a later
workflow.
