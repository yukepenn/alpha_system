# LabelSpec Form

Use this form to prepare or document a governed `LabelSpec`. The source of
truth is `../../governance/LABEL_SPEC.md`; this form does not redefine that
schema.

## Governance Handles

- `label_spec_id`: `<lspec_... generated by governance from complete content>`
- `alpha_spec_id`: `<aspec_... optional governance reference>`

## Horizon

- Horizon type:
- Horizon length:
- Horizon-end rule:
- Timezone:

## Path Rules

- Path observation source:
- Terminal-row rule:
- MFE/MAE or barrier metadata, if applicable:
- Forward data boundary:

Future observations are legal only inside the governed label horizon.

## Cost Model

- Cost model name:
- Spread adjustment:
- Commission assumption:
- Slippage assumption:
- Cost metadata source:

Cost metadata in this template is planning metadata only.

## Target And Stop Rules

- Target rule:
- Stop rule:
- Barrier priority:
- Tie handling:

## Availability Time

- `availability_time`:
- `label_available_ts` derivation rule:

Every emitted LabelValueRecord must carry `label_available_ts`.

## Forbidden Feature Overlap

- Label reference:
- Aliases:
- Transforms:
- Version handles:

These references must not appear as live feature inputs.

## Leakage Checks

Required checks:

- `label_as_feature`
- `availability_time`

Add any additional governed leakage metadata here:

- Additional checks:
- Expected audit evidence:

## StudySpec Consumption Notes

- Intended `StudySpec` label handle: `<lspec_...>`
- Related feature request handles: `<freq_...>`
- Related AlphaSpec handle: `<aspec_...>`
- Dataset scope notes:

A StudySpec consumes handles and dataset-scope metadata. It must not copy this
form into a parallel label schema.
