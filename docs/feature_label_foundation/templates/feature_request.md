# FeatureRequest Form

Use this form to prepare a governance `FeatureRequest`. The source of truth is
`../../governance/FEATURE_REQUEST.md`; this form does not redefine that schema.

## Governance Handles

- `feature_request_id`: `<freq_... generated by governance from complete content>`
- `alpha_spec_id`: `<aspec_... existing or planned governance reference>`

## Requested Inputs

- Canonical input views:
- Required fields:
- DatasetVersion scope:
- Instrument universe:
- Partition scope:
- Session or roll context:

## Formula Sketch

Describe the requested transform in enough detail for duplicate-exposure review
and later FeatureSpec drafting. Use canonical inputs only. Do not reference raw
provider files, provider fields, or local data-root paths.

## Availability Assumptions

State how each input is available and how a later FeatureSpec should derive
`available_ts`. Example shape:

```text
feature.available_ts = max(input.available_ts)
```

## Duplicate Or Equivalent Exposure Notes

Record the existing governance duplicate-exposure guard output:

- `guard`: `duplicate_exposure`
- `checked`: `<true|false>`
- `registry_status`:
- `registry_entries_checked`:
- `findings`:
- `summary`:

Unchecked duplicate-exposure metadata must not be treated as clean.

## Data Requirements

- Accepted DatasetVersion requirement:
- Canonical record types:
- BBO requirements and missingness handling:
- Dense-grid no-trade handling:
- Locked-test contamination metadata requirement:

## Approval Status

Use one governance status:

- `PENDING`
- `NEEDS_REVIEW`
- `BLOCKED_DUPLICATE`
- `APPROVED`

Only an `APPROVED` request with checked duplicate-exposure metadata can pass the
FeatureRequest gate. This form itself is not implementation permission.
