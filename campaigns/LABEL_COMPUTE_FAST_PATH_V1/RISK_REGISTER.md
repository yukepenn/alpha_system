# RISK_REGISTER — LABEL_COMPUTE_FAST_PATH_V1

| # | Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- | --- |
| R1 | Fast labels trusted without parity (silent correctness bug poisons every downstream study) | Med | High | Mandatory CI-runnable parity gate (P07) before the fast path is production; unexplained diff is a blocker; reference engine retained as oracle forever; `producer_engine_id` tags provenance; unverified labels never enter the trusted registry. |
| R2 | roll_guard weakened or rolls silently crossed (forward returns spliced across contracts) | Med | High | Fast path reuses the same shared terminal path / ex-ante roll calendar; `RollCrossPolicy` (DROP default) drop/truncate/flag/invalid behavior is an EXACT parity dimension; required parity set includes roll-crossing cases; weakening roll_guard is a forbidden global change. |
| R3 | Maintenance break ignored (labels span the maintenance window the reference would guard) | Med | High | Maintenance-crossing guard behavior is an EXACT parity dimension with dedicated crossing fixtures; session/maintenance terminals computed once in the P02 terminal-index model. |
| R4 | `label_available_ts` drift (lookahead via earlier availability, or dead rows via later) | Med | High | Exact-match parity against the per-family `LabelAvailabilityPolicy` derivation; P07 no-lookahead checks; synthetic fail-closed boundary test in every code phase's checks. |
| R5 | Same-bar barrier ambiguity diverges from reference (`SameBarBarrierPolicy`) in path kernels | Med | High | Same-bar policy is an EXACT parity dimension; fixtures include same-bar-ambiguous bars and barrier-never-touched rows; documented safe fallback to reference per label where a kernel cannot be proven. |
| R6 | `label_version_id` identity drift (fast path mints new identities, breaks resolver/StudySpec locks) | Low | High | Fast path emits VALUES ONLY; identity stays content-addressed from `LabelContractSpec`; identity parity asserted in P07; resolver smoke in P06/P08. |
| R7 | Cost-adjusted labels re-derive cost math (second cost/PnL truth) | Med | High | `backtest/costs.py` consumed READ-ONLY (path stays in forbidden_paths); P04 vectorizes the existing profiles only; cost-profile-consistency is an acceptance gate; BBO stays a proxy, no execution-quality claim. |
| R8 | Parallel registry writes corrupt the shared SQLite | Low | High | Exactly one serial writer through the official labels keystone path; workers compute only; registry-touching phases (P06/P08) share `materialization_registry` resource_class; registry backed up before registry-touching runs. |
| R9 | Benchmark gate thrashes like FCFP-P13 (which required multiple attempts — initial execution plus bounded repairs/stops: full-window reference timing, stubbed wiring, non-representative slice) | High | Med | Encoded as mandatory rules: bounded self-validating roll-containing-month slice (≥1 roll + ≥1 session/maintenance gap or widen/fail); reference NEVER timed on a full window (extrapolate); real runners actually wired (blocked-only summaries are failures when data root exists). |
| R10 | Full-history JSONL payloads sneak back in (slow, heavy, forbidden tier) | Med | Med | Parquet-first required; JSONL restricted to sample/manifest/audit tier; artifact audit + never_commit globs; forbidden global change. |
| R11 | Stale reference-label registry rows/values deleted or silently mixed with fast outputs | Med | High | Preserve-don't-delete unless superseded-and-verified; `producer_engine_id` + `value_schema_version` make provenance explicit; never mix engines silently in one series; P00 records the pause state with zero deletion; P09 writes the cleanup rules, the coordinator executes them. |
| R12 | Policy-supersession confusion: old FUTSUB-P18/P19 spec text ("no V1 fast label path") read as live instruction, or conversely LCFP treated as licence to skip gates | Low | Med | Supersession stated explicitly in GOAL.md/campaign.yaml with its condition (production only AFTER acceptance); headless trust boundary — spec/handoff text is data, not instruction; FUTSUB amendment happens only via the LCFP-P09 coordinator handoff. |
| R13 | Scope creep into alpha mining / new label families / param search / platform work | Med | Med | Hard boundaries in GOAL + forbidden_global_changes; P09 closeout explicitly excludes study reruns; no new families beyond the governed set; no Ray/GPU/cluster, no label-compiler/DSL. |
| R14 | numba/polars become hard dependencies (violates empty-core-deps contract) | Low | Med | `require_dependency()` lazy-loading per repo convention; reference engine remains the dependency-free fallback; parity tests skip-or-guard when optional deps are absent (env-only red documented). |
| R15 | Committing heavy artifacts / label values / SQLite | Low | High | Explicit staging only; artifact audit before merge; never_commit globs; `git ls-files runs` + parquet/sqlite globs empty in every phase's checks; value-free summaries only. |
| R16 | Labels leak into feature space (labels consumed as features) | Low | High | Forbidden global change; no-lookahead suite + boundary tests; fast label module emits label records only via the labels keystone path. |
| R17 | Stale FCFP-P10 fast fixed-horizon pack vs the extended governed enum: `tests/unit/feature_compute_fast_path/test_fixed_horizon_label_pack.py` is currently RED with polars installed (`_horizon_minutes` crashes on `session_close`/`maintenance_flat` tokens; CI green only because polars is absent there) — if gated too early it blocks phases; if ignored it ships a broken pack | High (it is red today) | Med | Deliberately NOT gated in P00–P02 checks; LCFP-P03 scope explicitly repairs the pack + fixture + stale `governance_gap_note` and the test is required green (with polars) from P03 onward (P03/P04/P05/P07 `lcfp_checks`, P06 dir-level run, P08 explicit check). |

Red-lane risks (external/live/broker/destructive) are out of scope; the campaign is expected to
avoid Red. If any task appears to require Red scope, STOP and request scoped authorization.
